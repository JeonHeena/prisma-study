version: '3.8'
services:
  postgres:
    image: postgres:13.5
    restart: always
    environment:
      - POSTGRES_USER=heena
      - POSTGRES_PASSWORD=heenapassword
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - '5433:5433'

volumes:
  postgres:
